/**
 * Temporary stub for MCP server while we resolve MCP SDK installation issues
 * This allows the package to build and the cleanup process to continue
 */

console.log('Nodash MCP Server - Currently under maintenance');
console.log('The MCP server functionality is temporarily disabled while we resolve dependency issues.');
console.log('Please check back later or use the SDK and CLI tools directly.');

process.exit(0);